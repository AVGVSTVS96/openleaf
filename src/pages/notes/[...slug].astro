---
import { ErrorBoundary } from "../../components/ErrorBoundary";
import Footer from "../../components/Footer.astro";
import { NotesAppWithProvider } from "../../components/notes/NotesAppWithProvider";
import Layout from "../../layouts/Layout.astro";

// Disable prerendering - all routes handled client-side
export const prerender = false;
---

<Layout title="Notes - OpenLeaf">
  <ErrorBoundary>
    <NotesAppWithProvider client:load />
  </ErrorBoundary>
  <Footer />
</Layout>
